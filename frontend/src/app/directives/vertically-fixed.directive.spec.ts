import { VerticallyFixedDirective } from './vertically-fixed.directive';

describe('VerticallyFixedDirective', () => {
  it('should create an instance', () => {
    const directive = new VerticallyFixedDirective();
    expect(directive).toBeTruthy();
  });
});
