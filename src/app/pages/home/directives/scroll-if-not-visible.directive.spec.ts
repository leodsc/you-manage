import { ScrollIfNotVisibleDirective } from './scroll-if-not-visible.directive';

describe('ScrollIfNotVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollIfNotVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
